CREATE PROCEDURE bin.testing
AS
BEGIN
	SET NOCOUNT ON;

	-- Char of
	-- If I upcase the word char above, highlihting fails?
	-- '  The previous mucks up syntax highlighting by escaping the quote, so added one here.
	-- If I put the word "line" above, highlighting fails?

END	--	bin.testing
GO
